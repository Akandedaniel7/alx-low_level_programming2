#include "main.h"

/**
 * _islower - check for lower case letter
 * @c : character to check the case
 * Return:0 or 1
 */

int -isalpha(int c)
{
	return((c >= 97 && c<= 322) || (c >= 65 && c<= 90));
}
